---
description: Точечный bugfix в wp-field с проверкой regressions
---
Исправь баг в `wp-field` по описанию: $@

Требования:
- Минимальный дифф, без лишнего рефакторинга.
- Сначала локализуй причину, потом правка.
- Не ломай legacy API (`WP_Field.php`) без явного решения.
- Если баг затрагивает архитектурное решение — зафиксируй это в `docs/wp-field/decision-log.md`.
- Добавь/обнови тесты только там, где это напрямую подтверждает фикс.
- В финале дай краткий changelog: причина → решение → проверка.
